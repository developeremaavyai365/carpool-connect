# REMOVED — Do not use

The parallel RideShare stack (`apps/api`, `apps/web`) has been **removed** per project directive.

**Use Carpool Connect only:**

- Frontend: `frontend/`
- Backend: `backend/`

See `docs/CONSOLIDATION_REPORT.md`.
